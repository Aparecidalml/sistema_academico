<%- include('partials/header', { titulo: 'Página Inicial' }) %>

    <h1>Bem-vindo <%= usuario.nome %>!</h1>

    <nav>
        <% let perfil = usuario.perfil %>
        <!-- <% if( perfil === 'Admin' || usuario.perfil === 'Professor') { %>
            <a href="\cursos"> Visualizar Cursos</a>
            <a href="\cadastro"> Cadastrar Cursos</a>
        <% } else { %>
             <a href="\cursos"> Visualizar Cursos</a>
        <% } %> -->
    </nav>

       <nav>
        <% switch(perfil) { 
            case 'Admin':
            case 'Professor': %>
                <a href="\cursos"> Visualizar Cursos</a>
                <a href="\cadastro"> Cadastrar Cursos</a>
            <% break 
            case 'Estudante': %>
                <a href="\cursos"> Visualizar Cursos</a>
                <% break 
            default: %>
                
        <% } %>
    </nav>



    <br><br><br><br>

<%- include('partials/footer') %>